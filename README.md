# assets-internal

Public bucket for hosting images (PR/issue screenshots) via GitHub release assets.
Upload via the `imgur-upload` / image-upload skill; URLs look like:
`https://github.com/cline/assets-internal/releases/download/assets/<file>`
